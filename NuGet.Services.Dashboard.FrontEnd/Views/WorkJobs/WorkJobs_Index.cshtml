@using System;
@using System.Collections.Generic;
@using System.Linq;
@model List<Tuple<string, string, string,string>>
<div class="fg-color-darken">
    <table class="hovered">
        <tbody>
            <tr>
                <th>
                    JobInstanceName
                </th>
                <th>
                    Status
                </th>
                <th>
                    Last completed
                </th>

            </tr>
            @if (@Model != null)
            { 
            foreach (Tuple<string, string, string,string> jobDetail in @Model)
            {
                <tr>
                    <td>
                       <a href="@jobDetail.Item2" target="_blank">@jobDetail.Item1</a> 
                    </td>
                    <td>
                        @if (jobDetail.Item3.Equals("True"))
                        {
                            <span class="square bg-color-green"></span>
                        }
                        else
                        {
                            <span class="square bg-color-red"></span>
                        }
                    </td>
                    <td>
                        @jobDetail.Item4
                    </td>
                </tr>
            }
            }
        </tbody>
    </table>
</div>

